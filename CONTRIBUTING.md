# Contributing

1. Create a branch for each change.
2. Add or update regression tests.
3. Run `python -m pytest -q`.
4. Do not commit credentials, client data, caches or generated output.
5. Preserve point-in-time provenance and document any limitation.

Pull requests should explain the evidence for behavioral changes and list any
remaining failures honestly.
